# ai-memory 修 bug 场景配置
MODE="team"
# 用法: bash engine/lea.sh scenarios/ai-memory-fix.conf

PROJECT_DIR="/mnt/d/桌面文件夹/ai-memory--副本实验"

GOAL="修复项目中 land() 方法返回已被 supersede 的记忆节点的问题。land() 应该过滤掉 supersededBy != null 的节点，和 _spread() 的逻辑保持一致。"

# 验证：跑现有测试套件，pass 数不降 = 成功
VERIFY_CMD="node --test eval/test-engine.mjs eval/test-html.mjs eval/test-errors.mjs eval/test-index.mjs"
VERIFY_TYPE="test"
SUCCESS_CRITERIA="pass>=85"

MAX_ROUNDS=5
