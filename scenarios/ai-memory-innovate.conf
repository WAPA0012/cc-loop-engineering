# ai-memory 创新优化场景
MODE="team"
# 让 LEA 的创意者搜索外部方案，提出记忆检索算法的改进

PROJECT_DIR="/mnt/d/桌面文件夹/ai-memory--副本实验"

GOAL="优化 ai-memory 的记忆检索质量。当前 land() 和 searchAndScore() 用的是简单的关键词匹配 + 标签重叠打分。调研业界更好的记忆/知识检索方法（如向量检索、语义相似度、图算法等），提出一个可行的改进方案。注意：只提方案和分析，不要求本次实现。"

# 这个场景主要是创意者干活，gate 只确保不破坏现有测试
VERIFY_CMD="node --test eval/test-engine.mjs eval/test-html.mjs eval/test-errors.mjs eval/test-index.mjs"
VERIFY_TYPE="test"
SUCCESS_CRITERIA="pass>=85"

MAX_ROUNDS=5
